*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab

# Signing material must never be committed. Losing the keystore means never
# being able to update the app on Play under the same identity.
keystore.properties
*.jks
*.keystore

# Session notes live in the parent folder, outside both repositories.
CLAUDE.md
